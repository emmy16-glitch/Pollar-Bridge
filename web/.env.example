# Copy to .env and fill in. Publishable key ONLY — never put a secret here.
VITE_API_URL=http://localhost:4000/api
VITE_POLLAR_PUBLISHABLE_KEY=
# Operator key for money-moving buttons (must match backend OPERATOR_API_KEY).
# Leave empty for local demo.
VITE_OPERATOR_KEY=
